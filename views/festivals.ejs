<% include partials/header %>


<h1>This is the festivals page!!!</h1>

<a href="/festivals/new"> Add New Festival</a>
<ul>
    <% festivals.forEach(function(festival){ %>
    <div>
         <h4><%= festival.name;  %></h4>
         <img src = "<%= festival.image%>">
    </div>
   
<% }); %>
</ul>

<% include partials/footer %>
